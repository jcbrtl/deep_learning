#include <bits/stdc++.h>
using namespace std;

int main() {
	// It is a general method that can be used to solve almost any algorithmic problem. The idea is to 
	// generate all possible solutions to the problem using brute force, and then select the best solution
	// or count the number of solutions, depending on the problem.
	
	// Generating subsets
	// phi, {0}, {1}, {2}, {0,1}, {0,2}, {1,2}, {0,1,2} for {0,1,2}
	
	// Method 1 -> recursion
	//void search(int k) {
		//if (k==n) {
			 process subset
		//}
		//else {
			//search(k+1); // subset is a vector
			//subset.push_back(k);
			//search(k+1);
			//subset.pop_back(); 
		//}
	//}
	
}
